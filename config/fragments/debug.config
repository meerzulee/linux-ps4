# Debug Configuration Fragment
# Enable these options for kernel debugging
#
# WARNING: These options increase kernel size and reduce performance
# Only enable during development/debugging

# Kernel debugging
CONFIG_DEBUG_KERNEL=y
CONFIG_DEBUG_INFO=y
CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT=y

# Printk / console - CRITICAL for seeing boot errors
CONFIG_PRINTK=y
CONFIG_PRINTK_TIME=y
CONFIG_CONSOLE_LOGLEVEL_DEFAULT=7
CONFIG_CONSOLE_LOGLEVEL_QUIET=4
CONFIG_MESSAGE_LOGLEVEL_DEFAULT=4

# Early printk (useful for boot issues)
CONFIG_EARLY_PRINTK=y
CONFIG_EARLY_PRINTK_DBGP=y

# Kernel log buffer - larger for capturing boot issues
CONFIG_LOG_BUF_SHIFT=18
CONFIG_LOG_CPU_MAX_BUF_SHIFT=12

# Magic SysRq (emergency debugging)
CONFIG_MAGIC_SYSRQ=y
CONFIG_MAGIC_SYSRQ_DEFAULT_ENABLE=0x1

# Dynamic debug
CONFIG_DYNAMIC_DEBUG=y
CONFIG_DYNAMIC_DEBUG_CORE=y

# Stack traces
CONFIG_STACKTRACE=y
CONFIG_FRAME_POINTER=y

# Boot parameter: Add these to bootargs.txt for verbose boot:
#   loglevel=7 debug ignore_loglevel earlyprintk=vga
#   drm.debug=0x1e (DRM debugging)
#   initcall_debug (trace init calls)

# Panic behavior - don't reboot immediately on panic
CONFIG_PANIC_TIMEOUT=0
CONFIG_PANIC_ON_OOPS=y

# PS4/DRM/GPU debugging
CONFIG_DRM_DEBUG_MODESET_LOCK=y

# Memory debugging (optional, very slow)
# CONFIG_DEBUG_SLAB=y
# CONFIG_DEBUG_PAGEALLOC=y
# CONFIG_DEBUG_OBJECTS=y

# Lock debugging (optional)
# CONFIG_DEBUG_SPINLOCK=y
# CONFIG_DEBUG_MUTEXES=y
# CONFIG_DEBUG_LOCK_ALLOC=y
