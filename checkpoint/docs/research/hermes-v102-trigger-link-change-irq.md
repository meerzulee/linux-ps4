# Hermes (gpt-5.5) — v102 trigger LINK_CHANGE IRQ — 2026-05-13

Finding: Ghidra does not show software explicitly raising `BAR+0x050[2]`; it is emitted by the MAC when the decoded `BAR+0x04` link tuple changes.  Evidence: `mts_intr` treats IRQ bit 2 only as notification: call `mts_link_change`, reread `BAR+0x04`, then enqueue `gbe:phy_ctrl` event `0x100` if bit0 is up else event `1` (`/tmp/v93-ghidra/mts_intr.c:222-239`).  `mts_link_change` itself only reads `BAR+0x04` and decodes bit0/link, bits3:2 speed, bit4 duplex, bit8 extra media flag into ifmedia state (`/tmp/mts_link_change.c:12-68`).  No ack/rearm write to a “link IRQ source” appears there.

So answer is closest to (c): an internal MAC/PCS status-transition interrupt, not purely PHY AN complete.  PHY AN is only upstream stimulus.  The IRQ likely fires when the MAC’s synchronized link tuple changes from down/unknown to `0xb19`-style up+speed+duplex.  If v97 receives packets but stays `0xb18`, RX DMA is alive while the MAC TX/PCS link detector never crosses the “link-up” edge.

Best v100 way to provoke a fresh bit2 after `ndo_open`: copy Orbis `gbe:phy_ctrl` event-1 recovery, not BAR roulette.  While IRQ bit2 is unmasked, if `BAR+0x04[0]==0`, read C22 reg9/reg4, ensure reg9 `0x200` and reg4 `0x180`, then read BMCR reg0 and write `BMCR | 0x1200` to restart AN (`/tmp/mts_phy_ctrl.c:119-147`).  Also try the timeout path’s reg9 toggle then BMCR restart (`lines 94-105`) to create a real PHY/PCS edge.  I would not toggle MAC_CTRL3 first; Orbis only programs it during `mts_mac_init`, not as link IRQ rearm.
